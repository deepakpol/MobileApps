{
    "Context/add_shortcuts/Automation_scripting_rules_map.json": [
        ...
        {
            "correlation_steps_to_scripts": [
                {"test_step": "Reset HPX application to default state and ensure user is logged in before launching.", "mapped_script_line": "self.fc.reset_app()"},
                {"test_step": "Add supported printer device via 'Add Device' button on root view screen, searching by IP address, and verify printer is listed and ready.", "mapped_script_line": "self.hpx_printer_details.click_add_device_btn() → self.printers.search_printer_by_ip(self.p.ipAddress)"},
                {"test_step": "After each navigation (root view, device details, shortcuts screen, add new shortcuts screen), assert that the expected screen title and main UI elements are visible and correctly rendered for early failure detection.", "mapped_script_line": "assert self.hpx_shortcuts.verify_shortcuts_screen_title(), assert self.hpx_shortcuts.verify_add_new_shortcut_screen_title()"}
            ],
            "intermediary_steps": [
                {"test_case_step": "Reset application to initial state before launching HPX app", "automation_script_step": "self.fc.reset_app()", "steps": ["[Step 1] Initialize the test environment and ensure clean state", "[Step 2] Invoke the reset_app() method from the flow controller", "[Step 3] Verify application returns to initial state successfully"]},
                {"test_case_step": "Add printer device (precondition) via Add Device button and search by IP address", "automation_script_step": "self.hpx_printer_details.click_add_device_btn() → self.printers.search_printer_by_ip(self.p.ipAddress)", "steps": ["[Step 1] Navigate to the printer management section on root view", "[Step 2] Locate and click the 'Add Device' button", "[Step 3] Access the printer search functionality", "[Step 4] Input the printer IP address from test configuration", "[Step 5] Execute search and verify printer is discovered and added successfully"]},
                {"test_case_step": "Verify shortcuts screen is displayed after navigation", "automation_script_step": "assert self.hpx_shortcuts.verify_shortcuts_screen_title()", "steps": ["[Step 1] Retrieve the shortcuts screen title element", "[Step 2] Verify the title matches expected shortcuts screen identifier", "[Step 3] Assert validation passes confirming shortcuts screen is displayed"]},
                {"test_case_step": "Verify add new shortcut screen is displayed after clicking Add New Shortcut button", "automation_script_step": "assert self.hpx_shortcuts.verify_add_new_shortcut_screen_title()", "steps": ["[Step 1] Retrieve the add new shortcut screen title element", "[Step 2] Verify the title matches expected add new shortcut screen identifier", "[Step 3] Assert validation passes confirming add new shortcut screen is displayed"]}
            ],
            "automation_scripts_rules": [
                {"rule_id": "NEW-RUL-001", "description": "Explicitly document implicit steps in the test case for traceability (reset app, printer addition, screen verifications)", "applies_to": "All test case automation scripts", "rule_definition": ["WHEN: Test case contains implicit steps required for execution", "THEN: Document these steps explicitly in the automation mapping for traceability", "IMPLEMENTATION: Include reset app, printer addition, and screen verification steps as explicit intermediary steps"]},
                {"rule_id": "NEW-RUL-002", "description": "Ensure all supporting validation steps (screen title assertions) are included in automation scripts for early failure detection", "applies_to": "All navigation and screen transition steps", "rule_definition": ["WHEN: Navigating between screens or views", "THEN: Assert screen title and main UI elements are visible and correctly rendered", "IMPLEMENTATION: Add assertion steps after each navigation to verify screen title and UI elements"]},
                {"rule_id": "NEW-RUL-003", "description": "Maintain strict sequential execution order and intermediary step granularity in automation implementation", "applies_to": "All automation script steps within a test case", "rule_definition": ["WHEN: Multiple steps exist in automation script", "THEN: Execute in strict sequential order and maintain intermediary step granularity", "IMPLEMENTATION: Follow execution order and ensure intermediary steps are detailed for each script line"]}
            ]
        }
    ]
}